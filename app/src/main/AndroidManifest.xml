<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.supermai">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_build_24px"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_build_24px"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DataWLender" />
        <activity android:name=".DataDLender" />
        <activity android:name=".DataGLender" />
        <activity android:name=".CategoriasActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
    </application>

</manifest>